class Solution:
    def isPalindrome(self, x: int) -> bool:
        length=len(str(x))
        
        for i in range(0,length//2):
            if str(x)[i]!=str(x)[length-i-1]:
                return False
        return True        
